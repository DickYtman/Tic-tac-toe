
function profile(){
    const selectedSymbol = document.querySelectorAll(".imgPlayer, .idImg");
    selectedSymbol.forEach(imgPlayer =>{
imgPlayer.addEventListener("click", e=>{
    let target= e.target
}
)
    })
}

profile();